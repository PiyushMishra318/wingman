# Legacy Python prototype

These files are the original `app-updater` script and helpers. The supported application is the Go implementation in the repository root (`cmd/wingman`).

Not built or tested as part of CI.
